defaultBackend:
    nodeSelector:
        beta.kubernetes.io/os: linux

controller:
    replicaCount: 2
    nodeSelector:
        beta.kubernetes.io/os: linux
    
    admissionWebhooks:
        patch:
            nodeSelector:
                beta.kubernetes.io/os: linux

    service:
        # aquired through azure public ip service
        loadBalancerIP: "20.105.180.169"
        annotations:
            service.beta.kubernetes.io/azure-dns-label-name: "rasax-gpmt"

